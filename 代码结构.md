data_share_frontend/
├─ frontend/                    # 前端主项目，真正运行的代码在这里
│  ├─ src/
│  │  ├─ api.js                 # 后端 API 地址、axios 实例、请求 token 拦截器
│  │  ├─ main.js                # Vue 应用入口，挂载 Pinia / Router / Vuetify
│  │  ├─ App.vue                # 根组件，包含顶部导航和 router-view
│  │  ├─ router.js              # 路由表、登录权限守卫
│  │  ├─ store/
│  │  │  └─ auth.js             # 登录状态，token/user 存 localStorage
│  │  ├─ plugins/
│  │  │  └─ vuetify.js          # Vuetify UI 组件库配置
│  │  ├─ services/
│  │  │  └─ s3Upload.js         # S3 直传、删除、objectKey 生成、错误分类
│  │  ├─ components/
│  │  │  └─ layout/
│  │  │     ├─ AppHeader.vue    # 顶部导航栏，登录/退出
│  │  │     └─ HomeMenuDrawer.vue # 左侧菜单
│  │  └─ pages/
│  │     ├─ Home.vue            # 主页面布局，包含侧边栏和子路由出口
│  │     ├─ Login.vue           # 登录 / 注册
│  │     ├─ Market.vue          # 数据市场
│  │     ├─ DatasetDetail.vue   # 数据集详情、申请共享
│  │     ├─ RegisterData.vue    # 数据登记，S3 上传后登记元数据
│  │     ├─ MyData.vue          # 我的数据、共享审批、下载
│  │     └─ MyAccount.vue       # 我的账户、Token 展示/重生成
│  ├─ package.json              # 前端依赖和 npm scripts
│  ├─ package-lock.json         # 依赖锁定文件，建议保留
│  ├─ vite.config.js            # Vite 配置，路径别名 @ 指向 src
│  ├─ index.html                # Vite HTML 入口
│  ├─ jsconfig.json             # IDE 路径提示，可选但建议保留
│  └─API接口文档.md            # 后端接口说明文档，可选
├─ mock_backend.py              # 本地 mock 后端，可选，不是前端生产运行必需
├─ README.md                    # 根目录说明，可选，且内容有些地方可能过期
└─ DEPLOY.md                    # 部署说明，可选
